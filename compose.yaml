services:
  nuvio:
    build: .
    restart: unless-stopped
    ports:
      - 3000:3000
    healthcheck:
      interval: 30s
      retries: 3
      start_period: 5s
      test: ["CMD", "/app/dist/healthcheck"]
      timeout: 30s
