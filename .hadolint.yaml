ignored:
  # DL3008 (pin apt versions). ca-certificates is the only package installed,
  # and pinning it freezes the CA bundle at whatever the base image shipped
  # with : the one thing that should float. The `debian:bookworm-slim` tag is
  # the pin that matters here.
  - DL3008
  # DL3066 (non-numeric user id). `USER app` resolves fine: the `useradd` two
  # lines above creates it in this same image with a fixed `--uid 10001`.
  - DL3066
